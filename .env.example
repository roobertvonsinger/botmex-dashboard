# BD local (dev) — ajusta la ruta según tu workspace
BETMEX_DB=../Telegram/betmexico_accounts.db

# BD en VPS
# BETMEX_DB=/opt/betmexico/bot/betmexico_accounts.db

# CapMonster API key (prewarm + widget Pool)
CAPMONSTER_KEY=

# Contraseña maestra (bypass por usuario — no commitear)
BMX_MASTER=

# Solo para tests/dev: omite autenticación
# BMX_WEB_AUTH_MODE=open
