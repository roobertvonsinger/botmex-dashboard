# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Env
.env
.env.local

# Datos sensibles / scratch local — NUNCA commitear (xlsx con datos de tarjetas)
reports/
idea_vaga.txt

# Passwords
data/

# BD (no commitear datos reales)
*.db
*.db-shm
*.db-wal

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
# Ignorar Claude session/cache local, pero commit las skills del repo (parte de la bitácora)
.claude/projects/
.claude/cache/
.claude/sessions/
.claude/local/
.claude/*.log
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/settings.json

# Visual companion (brainstorm mockups)
.superpowers/

# Dev / preview temporales (C1)
.playwright-mcp/
.claude/launch.json
static/_depos_preview.html

# Graphify
graphify-out/
.graphify/

# CDP capture — tráfico real con JWTs/clabes, NUNCA commitear (solo scripts .py)
tools/*.jsonl
tools/*.log

# Visual inspect — screenshots efímeros del inspector Playwright
_screenshots/

# Memoria de sesion de herramienta local (Claude Code) + backups graphify
memory/
.claude/settings.json.graphify-bak
.obsidian/

# Bot legacy de Telegram (@betmexbot) — dueño: monorepo Proyectos/BetMexico/Telegram/.
# En KVM4 el contenedor `betmexico-bot` corre `python betmexico_bot.py` desde este
# mismo dir (bind-mount compartido con el dashboard). Estos módulos son copias
# desplegadas del monorepo, NO codigo del dashboard: ignorarlos para que
# `git status` en prod quede legible y un `git clean` no los borre.
/betmexico_bot.py
/betmexico_check.py
/betmexico_amazon.py
/betmexico_notes.py
/betmexico_search.py
/betmexico_reports.py
/betmexico_payments.py
/betmexico_withdrawals.py
/magicbox_v*.py
/wabox_v*.py
/models_fancy.py
/models_restored.py
/portal.html
/portal.js

# Scripts de mantenimiento one-shot desplegados a KVM4 (no son del dashboard)
/clean_and_check.py
/debug_proxies.py
/export_hunter*.py
/migrate_grades.py
/patch_capmonster.py
/print_schema.py
/scratch_inspect_max.py

# Backups del deployer manual en KVM4
*.bak-[0-9]*

