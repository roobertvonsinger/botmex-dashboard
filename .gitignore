# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Env
.env
.env.local

# Datos sensibles / scratch local — NUNCA commitear (xlsx con datos de tarjetas)
reports/
idea_vaga.txt

# Passwords
data/

# BD (no commitear datos reales)
*.db
*.db-shm
*.db-wal

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
# Ignorar Claude session/cache local, pero commit las skills del repo (parte de la bitácora)
.claude/projects/
.claude/cache/
.claude/sessions/
.claude/local/
.claude/*.log
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/settings.json

# Visual companion (brainstorm mockups)
.superpowers/

# Dev / preview temporales (C1)
.playwright-mcp/
.claude/launch.json
static/_depos_preview.html

# Graphify
graphify-out/
.graphify/

# CDP capture — tráfico real con JWTs/clabes, NUNCA commitear (solo scripts .py)
tools/*.jsonl
tools/*.log
